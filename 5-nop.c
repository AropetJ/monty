#include "monty.h"

/**
 * _nop - doesn’t do anything.
 *
 * @top: The top of the stack or queue
 * @i: A count to traverse the stack
 *
 * Return: No return value
 */

void _nop(stack_t **top, unsigned int i)
{
	(void) top;
	(void) i;
}
